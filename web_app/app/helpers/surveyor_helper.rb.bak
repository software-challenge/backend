module SurveyorHelper
  def update_my_survey_path
    @context.is_a?(Season) ? season_survey_token_survey_url(@context,@survey_token, :id => 0) : contest_survey_token_survey_url(@context,@survey_token,:id => 0)
  end

end
