<h1>Computerspieler hochladen</h1>
<h2>Team: <%= @school %> </h2>

<%= link_to "Computerspieler hochladen", new_contestant_client_url(@contestant) %>

<--
button: open dialog
get school name (s. above)
buttons instead of links
edit field in front of upload button
dialog öffnen -> pfad in edit schreiben
sort history by date
-->

<table border="1">
  <tr>
    <th>hochgeladen am</th>
    <th>Name</th>
    <th>von Teilnehmer</th>
    <th>hochgeladen von</th>
  </tr>
  <% @clients.each do |client| %>
    <tr>
      <td><%=h client.created_at %></td>
      <td><%=h client.name %></td>
      <td><%=h client.contestant.name %></td>
      <td><%=h client.file_file_name %></td>
      <td><%= link_to 'Edit', edit_contestant_client_url(@contestant, client) %></td>
    </tr>
  <% end %>
</table>