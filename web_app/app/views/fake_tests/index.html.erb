<div id="actions">
  <% link_to new_contest_fake_test_url(@contest) do %>
    <%= image_tag('actions/add.png') %> Neuer Plagiat-Test
  <% end %>
</div>


<h1> 
  <%= link_to @contest.name, contest_url(@contest) %> / 
  <%= link_to "Plagiat-Tests", contest_fake_tests_url(@contest) %>
</h1>

<% tabnav :administration do %> 
  <%= render :partial => 'list', :locals => {:fake_tests => FakeTest.all.select{|ft| ft.clients.first.contest == @contest}}%>
<% end %>
