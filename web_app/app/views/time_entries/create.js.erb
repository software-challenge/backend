<% unless @time_entry.new_record? %>
  //$("#time_entry_form_row_new").replaceWith("<%= escape_javascript(render :partial => "time_entry", :locals => {:time_entry => @time_entry}) %>")
  //$(".add_entry").show()
  window.location.reload()
<% else %>
  $("#time_entry_form_row_new .flash_messages").html("")
  <% for error in @time_entry.errors.full_messages %>
    $("#time_entry_form_row_new .flash_messages").append("<div class='ui-widget flash_error' style='padding:4px;'><div class='ui-state-error ui-corner-all'><p><span class='ui-icon ui-icon-alert' style='float:left; margin-right: .3em;'></span><b>Fehler:</b> <%= error %></p></div></div>")
  <% end %>
<% end %>
